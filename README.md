# pyaesfile
This project provides a robust command-line tool for secure file encryption and decryption, utilizing the industry-standard AES-256-GCM (Galois/Counter Mode) authenticated encryption algorithm at its core.
